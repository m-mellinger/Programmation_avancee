SRC=main.c Affichage.c Initialisation.c
HEADERS=Affichage.h Initialisation.h
BIN=exec
LIBS=-L/usr/lib/x86_64-linux-gnu -lsndfile -lm

$(BIN): $(SRC) $(HEADERS) 
	gcc -Wall -g  -std=c99 $(SRC) -lm `sdl2-config --cflags --libs`  $(BIN)

clean:
	rm -f $(BIN)
